import React from 'react';
import {Container} from "react-bootstrap";

const DataStreams = () => {
    return (
        <Container fluid>
            
        </Container>
    );
};

export default DataStreams;